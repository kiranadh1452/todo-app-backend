// controller for user actions

const login = async (req, res, next) => {
    // some code to login the user
};

const signUp = async (req, res, next) => {
    // some code to sign up the user
};

const resetPassword = async (req, res, next) => {
    // some code to reset the password
};

const changePassword = async (req, res, next) => {
    // some code to change the password
};

const patchProfile = async (req, res, next) => {
    // some code to patch the profile
};

module.exports = {
    login,
    signUp,
    resetPassword,
    changePassword,
    patchProfile,
};
